import React from 'react'

const Login = () => {
    return (
        <div>Login</div>
    )
}

export default Login;

export const metadata = {
    title: "Login",
    description: "Welcome to Login",
};